{
  "name": "worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev -c wrangler.jsonc",
    "deploy": "wrangler deploy -c wrangler.jsonc",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260905",
    "@md-to-pdf/tsconfig": "workspace:*",
    "typescript": "^5.8",
    "vitest": "^3",
    "wrangler": "^4"
  }
}
